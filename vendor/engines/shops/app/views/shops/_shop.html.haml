%li
  %h3= t('shop')
  = link_to shop.title, shop
  .vendor
    = t('shop.vendor')
    %br
    = image_fu shop.user.image, :preview
    = shop.user.name
  = list shop, :products, :collection => shop.products.randomize.limit(2).without(@product)
